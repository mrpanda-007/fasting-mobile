# Fasting services

Future use cases live here: start a plan, end a phase, advance a rolling plan, restore an active fast, and derive a timer snapshot. Screens only request those actions and render their returned state.
