# Local persistence

Future implementations here persist the active fast, plans, and an outbound sync queue. The active fast must be written before the UI reports that a fast has started or changed phase.
